# ──────────────────────────────────────────────
# PG-RAG Secrets (TEMPLATE)
# ──────────────────────────────────────────────
# 1. Copy this file:  cp secret.example.yaml secret.yaml
# 2. Replace the placeholder with your real OpenAI API key
# 3. NEVER commit secret.yaml — it is gitignored
# ──────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: pgrag-secrets
type: Opaque
stringData:
  openai-api-key: "REPLACE_WITH_YOUR_OPENAI_API_KEY"
